type CvReview @model {
  comments: String
  createdAt: AWSTimestamp
  id: ID!
  lastUpdatedAt: AWSTimestamp
  lastUpdatedBy: String
  reviwedBy: String
  status: CvReviewStatus
  userId: String!
}

enum CvReviewStatus{
  submitted
  underReview
  reviewCompleted
}